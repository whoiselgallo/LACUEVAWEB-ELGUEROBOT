export const applyNeon = async (imageBuffer) => {
    // TODO: integrar con filtros de glow, duotone o blending modes
    // Ejemplo: sharp → tint + blur + composite

    console.log('💡 Aplicando iluminación neón cian/magenta...');
    return imageBuffer;
};
