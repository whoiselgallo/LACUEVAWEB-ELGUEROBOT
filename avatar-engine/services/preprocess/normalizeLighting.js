export const normalizeLighting = async (imageBuffer) => {
    // TODO: integrar con filtros de sharp o modelo de normalización

    // Placeholder
    console.log('🟨 Normalizando iluminación...');
    return imageBuffer;
};
