export const generateEmbeddings = async (imageBuffer) => {
    // TODO: integrar modelo de embeddings estilizados (no biométricos)
    console.log('🧬 Generando embeddings estilizados...');

    // Placeholder: vector aleatorio
    return [Math.random(), Math.random(), Math.random()];
};
